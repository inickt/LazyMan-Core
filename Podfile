# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def pods
  pod 'SwiftyJSON'
  pod 'Pantomime', :git => 'https://github.com/inickt/Pantomime.git', :branch => 'master'
end

target 'LazyMan Core iOS' do
  use_frameworks!
  platform :ios, '9.0'

  # Pods for LazyMan Core iOS
  pods
end

target 'LazyMan Core macOS' do
  use_frameworks!
  platform :osx, '10.10'

  # Pods for LazyMan Core macOS
  pods
end

target 'LazyMan Core tvOS' do
  use_frameworks!
  platform :tvos, '9.0'

  # Pods for LazyMan Core tvOS
  pods
end
